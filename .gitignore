# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
supabase/.temp/

# Claude Code internal files (session data, local settings) — skills are committed
.claude/settings.local.json
.claude/todos.json
.claude/worktrees/

# Environment files — never commit real secrets
.env
.env.*
!.env.example

# Use a single package manager (npm). Remove the other lockfiles.
bun.lockb
bun.lock
